# Включаем поддержку AndroidX (современные библиотеки Google)
android.useAndroidX=true
# Автоматически конвертируем старые сторонние библиотеки в AndroidX
android.enableJetifier=true

# Настройки памяти для сборки
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
